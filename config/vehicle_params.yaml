# Created by Nelson Durrant, Sep 2024
#
# VEHICLE-SPECIFIC ROS PARAMETER VALUES
# - When setting up a new vehicle, do not directly edit the file in 'templates'; rather the copied file generated by 'setup.sh' in 'config'
# - If you edit this file, you'll need to make sure the copied file in 'config' is updated as well

/**:
  ros__parameters:
    # water_salinity_ppt: 0.0 # 0.0 for fresh water, 35.0 for salt water
    # magnetic_declination: 10.7 # E is positive, W is negative, 10.70° E for Utah Lake
    # https://www.ngdc.noaa.gov/geomag/calculators/magcalc.shtml?

    vehicle_ID: 3

# coug3: # Change this to match the vehicle name

depth_convertor:
  ros__parameters:
    fluid_pressure_atm: 87250.0 # 87250.0 from testing, TODO: add units

# gps_odom:
#   ros__parameters:
#     origin.latitude: 40.238833 # 40.2735 for Utah Lake
#     origin.longitude: -111.742249 # -111.7126 for Utah Lake
#     origin.altitude: 1367.00 # from testing

dvl_a50_node:
  ros__parameters:
    dvl_ip_address: "192.168.2.95"

shallow_pressure_pub:
  ros__parameters:
    sensor_type: 0
    i2c_bus: 1
    frame_id: 'shallow_pressure_link'

deep_pressure_pub:
  ros__parameters:
    sensor_type: 1
    i2c_bus: 0
    frame_id: 'deep_pressure_link'

vehicle_pinger:
  ros__parameters:
    enable_ping: false
    ping_frequency_seconds: 4
    vehicles_in_mission: [1,2,15]

  # modem_ros_node:
  #   ros__parameters:
  #     seatrac_serial_port: "/dev/ttyUSB0" # TODO fix the port with the SBG plugged in

  # nmea_converter:
  #   ros__parameters:
  #     covariance_max_publish: 20.42


  